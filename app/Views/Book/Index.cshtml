@model IEnumerable<Book>
@{
    ViewData["Title"] = "Index";
}


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Book List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Book" asp-action="Create" class="btn btn-success">
                <i class="bi bi-plus-circle"></i> &nbsp; Create New Book
            </a>
        </div>
        <br />
        <br />
    </div>
</div>

<table class="table table-bordered table-striped" style="width: 100%">
    <thead>
       <tr>
           <th>
               Book Id
           </th>
           <th>
               Book Name
           </th>
           <th>
               Actions
           </th>
       </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td width="50%">
                    @obj.book_id
                </td>
                 <td width="50%">
                    @obj.book_name
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Category" asp-action="Edit" asp-route-id="@obj.book_id" class="btn btn-secodnary mx-2">
                            <i class="bi bi-pen-fill"></i> Edit
                        </a>
                        <a asp-controller="Category" asp-action="Delete" asp-route-id="@obj.book_id" class="btn btn-secodnary mx-2">
                            <i class="bi bi-trash-fill"></i> Delete
                        </a>
                    </div>

                </td>
            </tr>
        }
    </tbody>
</table>

